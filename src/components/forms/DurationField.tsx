import { useId } from 'react';
export function DurationField({ label, value, onChange, error, hint }: { label: string; value: string; onChange: (value: string) => void; error?: string; hint?: string }) { const id=useId(); const descriptionId=`${id}-description`; return <label htmlFor={id} className="block text-xs font-black uppercase tracking-[0.16em] text-hyrox-smoke">{label}<input id={id} inputMode="numeric" placeholder="hh:mm:ss" value={value} onChange={e=>onChange(e.target.value.replace(/[^\d:.]/g,''))} aria-invalid={Boolean(error)} aria-describedby={error||hint?descriptionId:undefined} className="mt-1 min-h-11 w-full rounded-xl border border-white/10 bg-black/40 px-3 py-2 text-base font-semibold text-white outline-none focus:border-hyrox-gold" />{error||hint?<span id={descriptionId} role={error?'alert':undefined} className={`mt-1 block normal-case tracking-normal ${error?'text-red-300':'text-white/50'}`}>{error ?? hint}</span>:null}</label>; }
